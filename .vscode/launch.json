{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug test_yield_curve",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/test_yield_curve",
      "args": [
        "-I${workspaceFolder}/include",
      ],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "build test_yield_curve"
    }
  ]
}
